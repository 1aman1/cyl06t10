#include <vector>
#include <iostream>

class Solution
{
public:
    bool exist(const std::vector<std::vector<char>> &board, std::string word)
    {
        std::vector<std::vector<bool>> visited(false);
        // initialise with negative, then mark true when , node is visited
    }
};

int main()
{
    std::vector<std::vector<bool>> visited(false);
    std::cout << std::boolalpha << visited[0][0];
    
    return 0;
}